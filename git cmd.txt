git config --global user.name "username"    configer user name    local to remote 
git config --global user.email "username_Email"  configer userEmail    local to remote


git config --list   : to show the user detail

git status : to check the git status
git init :  to initilize into git repo

file add
git add . - to track all files 

git commit -m "comments"- to coomit the file in local repo.

git log -to see how many times anyone modified your files..with commit id and user name +email +comments.

.gitignor : protect   from remote  serverth

git branch :  to check the branch 

git checkout -b "brach_name"   :   move to other branch or create a branchgit 

 git checkout master - wapas se master branch me aane k liye.

 git merge main- master waale se merge krne k liye.git
git branch -d new_Branch_Name - new jo branch bnaya h usko delete krne k liye

git clone (url) - kisi k code  ko clone k liye

git remote -v -fetch or push ki url dikhti h.

git remote add origin https://github.com/RiyaKeshri/learning.git-add krne k lye remote se local k liye

git push origin branch_name - push krne k liye folder ko central repo me.